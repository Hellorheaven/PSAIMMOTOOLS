<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="?android:windowBackground">

    <!-- Top App Bar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        style="@style/Widget.Material3.Toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.Material3.ActionBar"
        app:title="@string/app_name"
        app:menu="@menu/menu_main" />

    <!-- Spinner to select module -->
    <Spinner
        android:id="@+id/moduleSelector"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp" />

    <!-- Spinner for Bluetooth devices -->
    <Spinner
        android:id="@+id/bluetoothDeviceSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_marginHorizontal="8dp" />

    <!-- Connection button -->
    <Button
        android:id="@+id/connectButton"
        style="@style/Widget.PsaImmoTool.PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:text="@string/connect" />

    <!-- Module specific actions -->
    <Button
        android:id="@+id/requestPinButton"
        style="@style/Widget.PsaImmoTool.PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:text="@string/request_pin"
        android:visibility="gone" />

    <Button
        android:id="@+id/requestVinButton"
        style="@style/Widget.PsaImmoTool.PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:text="@string/request_vin" />

    <Button
        android:id="@+id/startCanListenButton"
        style="@style/Widget.PsaImmoTool.PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:text="@string/listen_can"
        android:visibility="gone" />

    <!-- Frame sending layout -->
    <LinearLayout
        android:id="@+id/sendFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:visibility="gone">

        <EditText
            android:id="@+id/inputFrameText"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:autofillHints="fr"
            android:hint="@string/enter_frame"
            android:inputType="text" />

        <Button
            android:id="@+id/sendFrameButton"
            style="@style/Widget.PsaImmoTool.PrimaryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/send_frame" />
    </LinearLayout>

    <!-- Log output -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:padding="8dp"
        android:text="@string/disconnected"
        android:textAppearance="@style/Widget.PsaImmoTool.SectionTitle" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:background="@color/md_theme_background"
        android:padding="8dp"
        android:overScrollMode="ifContentScrolls"
        android:scrollbars="vertical"
        android:fitsSystemWindows="true">

        <TextView
            android:id="@+id/outputText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textAppearance="@style/Widget.PsaImmoTool.SectionTitle" />
    </ScrollView>

    <!-- Export and Clear logs -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">

        <Button
            android:id="@+id/exportLogsButton"
            style="@style/Widget.PsaImmoTool.SecondaryButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/export_logs" />

        <Button
            android:id="@+id/clearLogsButton"
            style="@style/Widget.PsaImmoTool.SecondaryButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/clear_logs" />
    </LinearLayout>

</LinearLayout>
